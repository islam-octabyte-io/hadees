# Base URL of the hadees backend API.
# Server-side only — read in Server Components / Route Handlers, never shipped to the browser.
# Do NOT rename to NEXT_PUBLIC_* without also enabling CORS on the backend (see docs/adr/0001).
API_URL=http://localhost:3000
